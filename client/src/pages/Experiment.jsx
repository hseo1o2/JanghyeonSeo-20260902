// Phase 1: placeholder — full implementation in feat/candidate-cards
export default function Experiment() {
  return (
    <main style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', minHeight: '100vh' }}>
      <p style={{ color: 'var(--color-text-secondary)' }}>실험 화면 — 준비 중</p>
    </main>
  );
}
