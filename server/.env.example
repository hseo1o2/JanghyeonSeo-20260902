# Required
OPENAI_API_KEY=sk-...

# Set to your Vercel deployment URL in production (no trailing slash)
# Example: https://your-app.vercel.app
CLIENT_ORIGIN=http://localhost:5173

# Render injects PORT automatically — only set locally if needed
PORT=4000
